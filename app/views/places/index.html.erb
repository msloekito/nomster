<% @places.each do |place| %>
<div class="booyah-box col-xs-10 col-xs-offset-1"> 
    <h1><%= link_to place.name, place_path(place) %></h1>
    <i><%= place.address%></i><br /><br />
    <p><%= place.description %></p><br/>
    
    <% if place.user.present? %>
    <small><%= place.user.email %></small>
    <% end %>
    <% if place.comments.present? %>
      <div class="col-xs-12">
        <blockquote>
          <% if place.comments.last.humanized_rating.present? %>
            <b><%= place.comments.last.humanized_rating %></b> -
          <% end %>
          <%= place.comments.last.message %>
          <small><%= place.comments.last.user.email %></small>
        </blockquote>
    </div>
    <% end %>
    <% if place.photos.present? %>
      <div class="col-xs-12">

          <% if place.photos.last.present? %>
            <b><%= image_tag place.photos.last.picture, :class => "img-responsive center-block" %></b> -
          <% end %>
          <small><%= place.photos.last.caption %></small>
    </div>
    <% end %>
    </div>
<% end %>

<div  class="col-xs-8 col-xs-offset-2 apple_pagination">
    <p><%= will_paginate @places %></p>
</div>